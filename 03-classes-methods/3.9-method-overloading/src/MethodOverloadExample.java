public class MethodOverloadExample {
    /*
     * Результат выполнения и объяснение:
     *
     * В цикле for (1) всегда вызывается метод printNum(Number n),
     * поэтому вывод будет одинакового вида для всех элементов массива:
     *
     * Number=1
     * Number=11
     * Number=1.11
     * Number=11.11
     *
     * Это происходит потому, что тип переменной цикла — Number.
     * Перегрузка методов в Java определяется НА ЭТАПЕ КОМПИЛЯЦИИ,
     * и выбор метода зависит от статического типа аргумента,
     * а не от фактического типа объекта во время выполнения.
     *
     * В вызовах (2) методы выбираются по конкретному типу аргумента:
     *
     * - a.printNum(new Integer(1)) → printNum(Integer i)
     * - a.printNum(11)             → printNum(int i)
     * - a.printNum(1.11f)          → printNum(Float f)
     * - a.printNum(11.11)          → printNum(Number n),
     *   так как для типа double нет более подходящей перегруженной версии
     *
     * Таким образом, несмотря на одинаковые значения аргументов,
     * в цикле используется один и тот же метод из-за типа Number,
     * а в отдельных вызовах — разные перегруженные методы.
     */

}
