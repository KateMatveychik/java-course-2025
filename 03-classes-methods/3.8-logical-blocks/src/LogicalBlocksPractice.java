public class LogicalBlocksPractice {
    /*
     * Вывод программы:
     *
     * static logic
     * logic (1) id= 0
     * logic (2) id= 1
     * ctor id= 100
     *
     * Объяснение:
     * 1. Статический логический блок выполняется первым при загрузке класса
     *    и вызывается один раз, поэтому сначала выводится "static logic".
     *
     * 2. Затем начинается создание объекта A.
     *    Первый обычный логический блок выполняется до явной инициализации поля id,
     *    поэтому id имеет значение по умолчанию для int — 0.
     *
     * 3. После этого выполняется инициализация поля id значением 1,
     *    затем второй логический блок, который выводит "logic (2) id= 1".
     *
     * 4. В самом конце вызывается конструктор, где поле id присваивается значение 100,
     *    и выводится "ctor id= 100".
     */

}
